Субъекты:

    1) Клиент
        -пусто

    2) Роутерсервис (экс аппсервер) (обозвал так, потому что до этого было совсем ебаное название аппсервер, путающее с аппсервисом)
        -к нему подрубается клиент и через этот один коннект он общается со всеми аппсервисами (которые на этом роутере висят)
        -кол-во роутерсервисов неограничено, клиент тоже на одном коннекте к только одному роутеру не завязан
        -инфа об адресах роутеров берется у клиента из воздуха ( :) )
        -знает, какие сервисы на нем висят, и по запросу от клиента шлет ему список оных
        -ТОЛЬКО взаимодействие клиент<->аппсервис. тогда как аппсервис<->аппсервис и прочее через роутер не подразумевается

    3) Аппсервис (это название либы, но обзовем так само приложение):
        -имеет пять методов:
            -Register - регистрация пользователя в сервисе
            -Connect / Disconnect - подключение к сервису (=выход в онлайн)
                (коннект шлет клиент, тогда как дисконнект может послать и роутер (при отвале клиента))
                (так же коннект трактуется как переход клиента в режим получения сообщений)
            -Create - создание канала
            -Subscribe (Unsubscribe)- подписка (отписка) на канал
            -Send - послать в канал

            -так же в записях (которые мы писали) фигурирует разделение применения Subscribe и Send как на канал, так и на сервис
            (как это трактовать я не понял, поэтому нужно переобдумать)

    3.1) Идентитисервис:
        -тыщу (меньше) раз обсуждалась концепция идсервиса, каждый раз меняя ее и подходы/методы ее работы. финальной/полноценной нет
        -взял oauth 2, идентитисервис тут выступает авторизатором, т.е. выдает токен, куда этот токен себе пихать пока без понятия, за ресурсами эт куда то туда
        -сам протокол взаимодействия с идсервисом описан в протоколс.тхт
    
Готовое (сдуть пыль и потыкать палкой шоб не сдохло):
    1) роутерсервис
    2) либа для сервисов (аппсервис)
    3) протоколы общения (либа протоколс) (реализация интерфейсов коннекторов)
        - клиент-роутер через вебсокет-коннектор
        - остальное через обычный коннектор
    4) либа для идентитисервисов (тут сильнее тыкать палкой)
        - имплементировано взаимодействие в аппсервисом на уровне "лишь бы было"
Туду:
    1) перепридумать концепцию всего оркестра, потому что дутали того шо обсуждали до конца не вспомнить, а без них пиздец
    2) выработать четкие правила работы перепридуманной концепции
    3) записать четкие правила

    4) разные варианты протокола подключения (вебсокет, наш простой протокол) клиент<->роутер в рамках одного запущеного инстанса (сейчас либо/либо)
    5) реворк идентитисервиса под новую-старую концепцию

    6) безепуловые идентитисервис, аппсервис

    7) сами сервисы приложений
    8) клиент

Готовое прочее:
    1) коннекторы всех гендеров
    2) конфигуратор (реворкнуть детали)
    3) логгер (без логсервера)
    4) епул
